> At its core, PWA is still a web app that can be accessed via any web browser; it is discoverable and can be indexed by search engines and gain free organic traffic; it is built on top of URL and linkable by default, meaning it is zero-friction, zero-install, and easy to share. With the progressive enhancement, it can get many features that are only available for native apps, such as:  
> - installable: PWA can be installed as an independent app via the browser prompt or App Store
> - discoverable: PWA has its own icon on your desktop, dock or home screen on your devices after installation
> - standalone window: PWA has its own window after installation, so you won’t be lost in the sea of tabs in your browser
> - better performance: PWA can utilize service worker to cache assets and data, so you don’t have download the same assets and data over and over again from a remote server, drastically reduces the network latency and bandwidth cost, resulting in a more responsive user experience
> - offline support: PWA can work offline partially, as long as a page has been visited, you can continue to use it in read only mode when offline
> - push notification: PWA can send push notifications to users, to re-engage users and bring them back to the app
> - native integrations: PWA can utilize other native features of your devices, such as camera, microphone, GPS, etc. with the integration of [web APIs](https://web.dev/learn/pwa/capabilities#empowering_your_pwa), check [What PWA Can Do Today](https://whatpwacando.today/) for example [Xiao Hanyu, "Introducing PPResume PWA"]

译（Gemma 3 12B IT (Q5_K_M)）：  
本质上, PWA仍然是一个可以通过任何网络浏览器访问的网页应用；它具有可发现性，可以被搜索引擎索引，并获得免费的自然流量；它建立在URL之上，默认情况下可链接，这意味着它具有零摩擦、无需安装和易于分享的特性。通过渐进增强，它可以获得只有原生应用程序才能访问的许多功能，例如：
- 可安装：PWA可以通过浏览器提示或应用商店作为独立应用程序进行安装
- 可发现性：安装后，PWA在您的设备桌面、文档或主屏幕上会有自己的图标
- 独立窗口：PWA安装后拥有自己的窗口，因此您将不会在浏览器的标签页海洋中迷失方向
- 更好的性能：PWA可以利用服务工作者缓存资产和数据，因此您不必反复从远程服务器下载相同的资产和数据，从而大大减少网络延迟和带宽成本，带来更快的用户体验
- 离线支持：PWA可以在离线状态下部分工作，要访问过页面，就可以在离线模式下以只读方式继续使用
- 推送通知：PWA可以向用户发送推送通知，以重新吸引用户并将其带回应用程序
- 原生集成：PWA可以利用您设备的其它原生功能，例如摄像头、麦克风、GPS等,通过[Web API](https://web.dev/learn/pwa/capabilities#empowering_your_pwa)集成。要了解PWA今天能做什么，请查看[What PWA Can Do Today](https://whatpwacando.today/)作为示例

译（TranslateGemma 12B IT I1 (Q4_K_M)）：  
PWA的核心仍然是一个可以通过任何浏览器访问的网络应用程序；它可以被搜索引擎发现和索引，从而获得免费的自然流量；它基于URL构建，默认情况下可以链接，这意味着它无需安装、易于分享。通过渐进增强，它可以获得许多只有原生应用才具备的功能，例如:
> - 可安装：PWA可以通过浏览器提示或应用商店进行安装，作为独立的应用程序
> - 可发现性：安装后，PWA会在您的设备桌面、Dock或主屏幕上显示自己的图标
> - 独立窗口模式：安装后，PWA会拥有自己的窗口，这样你就不会在浏览器中大量的标签页中迷失方向
> - 性能更好：PWA可以利用Service Worker来缓存资源和数据，因此您无需反复从远程服务器下载相同的资源和数据，从而显著降低网络延迟和带宽成本，带来更流畅的用户体验
> - 离线支持：PWA可以在一定程度上离线工作。只要页面已被访问，即使在离线状态下，您也可以继续以只读模式使用它
> - 推送通知：PWA可以向用户发送推送通知，以重新吸引用户并让他们返回应用
> - 原生集成：PWA可以利用设备的其他原生功能，例如摄像头、麦克风、GPS等。通过集成 [Web API](https://web.dev/learn/pwa/capabilities#empowering_your_pwa)，您可以了解[PWA今天能做什么](https://whatpwacando.today/)作为示例