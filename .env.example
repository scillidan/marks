# Supports environment variables (Windows: %USERPROFILE%, Linux: $HOME).

# Paths to exclude from both PDF builds and the generated manifest.
# Path prefixes relative to the repo root, separated by spaces and/or semicolons.
GEN_EXCLUDE="chat/latex/;post/laws-of-software-engineering/"

# byya-nineveh/
#
# _site/gen.sh now compiles committed .typ files under
# byya-nineveh/<collection>/_output/typs/ directly, so BYYA_NINEVEH_SOURCE is
# only needed when running the initial generator: `just nineveh-md <subdir>`.
BYYA_NINEVEH_SOURCE=${USERPROFILE}/path/to

# byya-lyra/
#
# The .md sources are NOT committed; they are synced from the upstream
# BYYA-site repo (content.zh/docs/lyra-*), which also supplies nineveh's
# images. CI clones it automatically. For local builds, run:
#   uv run scripts/sync_byya_lyra_md.py --source <BYYA-site>/content.zh/docs
BYYA_SITE_DOCS=${USERPROFILE}/Share/projs-site/BYYA-site/content.zh/docs

# favorite-image/
#
# When not set, falls back to favorite-image/assets/ within the project.
# NOTE: favorite-image/assets/ is gitignored. For cloud builds, either commit the
# assets or set FAVORITE_IMAGE_SOURCE to a path that exists on the runner.
FAVORITE_IMAGE_SOURCE=${USERPROFILE}/path/to

# post/
#
# When not set, falls back to post/assets/ within the project.
POST_SOURCE=${USERPROFILE}/Local/Asset/cdn_image_post
# Upscaling model for small images. Optional; when unset, originals are copied.
UPSCAYL_MODEL=${USERPROFILE}/path/to/ncnn/models